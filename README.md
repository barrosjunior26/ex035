## ➕ Descrição do Aplicativo

Este aplicativo em C# realiza a **soma de dois valores**, garantindo que o usuário informe apenas números válidos. Ele utiliza `double.TryParse()` para validar a entrada e evitar erros, exibindo mensagens de correção sempre que necessário. Ao final, o programa mostra o resultado da soma com duas casas decimais.